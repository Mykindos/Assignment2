#include "PlayerShootBulletCommand.h"
#include "SceneManager.h"

/*
	Simple implementation of the Command pattern.
	Shoots a bullet when called.
*/
void PlayerShootBulletCommand::execute() {
	//SceneManager::getInstance()->addActor(ActorType::BULLET);
}